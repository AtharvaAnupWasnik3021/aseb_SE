[project]
name = "aseb-se"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "biopython",
    "fastapi",
    "numpy",
    "py3Dmol",
    "python-multipart",
    "requests",
    "scikit-learn",
    "scipy",
]
